/*
 * globals.c
 *
 *  Created on: 11.02.2019
 *      Author: dinkelsv64505
 */


#include "globals.h"

void MAC_init()
{
	//ein Schleifendurchlauf entspricht einer MAC-Adresse
	for (int i = 0; i < SIZEOFMAC; i++)
	{
		MAC_Ad[i][0] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 0);
		MAC_Ad[i][1] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 1);
		MAC_Ad[i][2] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 2);
		MAC_Ad[i][3] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 3);
		MAC_Ad[i][4] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 4);
		MAC_Ad[i][5] = FLASH_ReadByte(_MEMORY_MAC_BEGIN_ + (i * 6) + 5);
	}
}
